{
  "name": "bunkulator",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && html-minifier-terser index.html -o dist/index.html --collapse-whitespace --remove-comments --minify-css true --minify-js true"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
